"""Code Agent logic — Stage 2.

Will use PyGithub to inspect PRs, diff files, and query the
GitHub Actions API for pipeline status. Stub for now.
"""


def run_code_agent(question: str) -> dict:
    """Inspect GitHub PRs and CI/CD pipeline and answer code-related questions."""
    # TODO Stage 2: fetch PR diffs, workflow run statuses, deployment events
    return {
        "answer": "Code agent not yet implemented (Stage 2).",
        "sources": [],
    }
